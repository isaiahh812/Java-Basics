import jdk.javadoc.internal.doclets.formats.html.markup.Table;

public class buy {
    public static void main(String[] args) {
        // we have x amount of dollars
        // we wanna see what we can buy
        int ourMoney = 500;
        int car = 5000;
        int couch = 300;
        int tv = 600;
        int ps5 = 400;
        Boolean check = true;
        if (check == true) {
            int num = s.substring(s.length()-1, s.length());
            if(num % 2){
                check = true;
            }
            else{
                check = false;
            }
        }
        if(check){
            //do check for 3
            if(checkPasses){
                check = true;
            }
            else{
                check =false;
            }
        }
}
